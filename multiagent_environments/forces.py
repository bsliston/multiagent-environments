"""Basic forces manager for tracking interactions between entities.

Manager for tracking entities and computing the forces applied between them
once they physically contact one another.
"""

import numpy as np


class Entity:
    """"""

    def __init__(
        self,
        position: np.ndarray,
        velocity: np.ndarray,
        acceleration: np.ndarray,
    ):
        pass


if __name__ == "__main__":
    pass
